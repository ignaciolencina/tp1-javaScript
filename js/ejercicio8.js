// 8.- Escribe un programa que pida un número y diga si es divisible por 2

let numero = parseInt(prompt("Ingrese un número"));
if (numero % 2 === 0) {
  alert("El número " + numero + " es un número divisible en 2");
} else {
  alert("El número " + numero + " NO es un número divisible en 2");
}
